import 'package:flutter/material.dart';

class DisimpanScreen extends StatelessWidget {
  const DisimpanScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Ini halaman Disimpan"),
      ),
    );
  }
}
